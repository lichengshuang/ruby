#!/usr/bin/ruby

def max(a,b)
	if a > b
	#	a
		return "#{a} is big than #{b}"
		"#{a} is big than #{b}"
	else
	#	b
		return "#{b} is big than #{a}"
		"#{b} is big than #{a}"
	end
end


#p max(100,11)
p  max(100,11)
